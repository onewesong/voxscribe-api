# VoxScribe API 环境变量配置示例
# 复制此文件为 .env 并根据需要修改配置

# =============================================================================
# API 认证
# =============================================================================
# API认证令牌（可选，如果不设置则不进行认证）
# VOXSCRIBE_API_KEY=your_secret_api_key_here

# =============================================================================
# 并发和性能配置
# =============================================================================
# 最大工作线程数（默认：CPU核心数）
# 建议根据硬件配置调整：
# - 4核CPU: 4-8
# - 8核CPU: 8-16
# - 有GPU: 2-4（GPU处理时CPU线程需求较低）
MAX_WORKERS=4

# PyTorch线程数（默认：1）
# 建议设为1以避免线程过度订阅，除非你确定需要更多
TORCH_THREADS=1

# =============================================================================
# 硬件配置
# =============================================================================
# Whisper使用的设备（默认：auto）
# 可选值：auto, cpu, cuda, cuda:0, cuda:1, etc.
WHISPER_DEVICE=auto

# 是否启用模型缓存（默认：true）
# 启用缓存可提高性能，但会占用更多内存
ENABLE_MODEL_CACHE=true

# =============================================================================
# 文件处理配置
# =============================================================================
# 最大文件大小（字节，默认：50MB）
MAX_FILE_SIZE=52428800

# =============================================================================
# 日志配置
# =============================================================================
# 日志级别（默认：INFO）
# 可选值：DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# =============================================================================
# 高级配置（通常不需要修改）
# =============================================================================
# 如果你有多个GPU，可以设置CUDA可见设备
# CUDA_VISIBLE_DEVICES=0,1

# 设置OMP线程数（OpenMP，影响某些数值计算库）
# OMP_NUM_THREADS=1

# 设置MKL线程数（Intel Math Kernel Library）
# MKL_NUM_THREADS=1 